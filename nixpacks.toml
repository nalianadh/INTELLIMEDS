[phases.setup]
nixPkgs = [
  "php82",
  "php82Packages.composer",
  "python311",
  "nodejs_20",
  "nodePackages.npm",
  "gcc-unwrapped",
  "zlib"
]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "LD_LIBRARY_PATH=/nix/store/$(ls /nix/store | grep gcc | grep lib | head -1)/lib /app/.venv/bin/pip install -r requirements.txt",
  "composer install --no-dev --optimize-autoloader",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache",
  "chmod -R a+rw storage"
]

[start]
cmd = "bash start.sh"